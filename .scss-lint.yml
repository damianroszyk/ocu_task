linters:
    Indentation:
        enabled: false

    PropertySortOrder:
        enabled: false

    StringQuotes:
        enabled: false

    SelectorDepth:
        max_depth: 3

    NestingDepth:
        max_depth: 3

    SelectorFormat:
        convention: hyphenated_BEM

    NameFormat:
        convention: ^[0-9a-z\-_]+$
